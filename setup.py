from setuptools import setup, find_packages

setup(
    name="ragid_gate",
    version="0.1.0",
    description="Shared hard gate enforcement for RealAgentID, TrailStax, and Ira-Digital-Blueprints",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[],
)
